#!/usr/bin/env python
from setuptools import setup, find_packages;

setup(name='vorphost',
      version='1.1.0',
      description='Python daemon to know a host is physical or virtual',
      url='https://github.com/arblrjcl/vorphost.git',
      author='Ravi_A',
      author_email='arblr@instruction.com',
      license='Apache',
      packages=['vorphost'],
      zip_safe=False)

